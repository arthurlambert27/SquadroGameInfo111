#ifndef MANIPULATION_TABLEAU_H
#define MANIPULATION_TABLEAU_H

#include <vector>


/* Fonction qui permet de savoir si un tableau est nul ou pas.
* @param tab qui est le tableau a vérifié
* @return un bool true si le tableau est vide, false sinon
*/
bool tabIsNul(std::vector<int> tab);

#endif
